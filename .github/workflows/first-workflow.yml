name: First Workflow

on: push
jobs: 
    first-job:
        runs-on: ubuntu-latest
        steps:
        - name: Demo step
          run: echo "Welcome To GitHub Actions -FIRST WORKFLOW"
        - name: Checkout Repo
          uses: actions/checkout@v5 
        - name: Install cowsay
          run: sudo apt install cowsay -y     

        - name: Create Cow
          run: cowsay -f cow "I am eating grass" >> cow.txt
          run: cowsay "I am eating grass" >> cow.txt
        
        - name: Show Cow
          run: cat cow.txt    